import React, { PropsWithChildren } from 'react'

const ChekcoutLayout = ({ children }: PropsWithChildren) => {
    return (
        <>
            {children}
        </>
    )
}

export default ChekcoutLayout